#include "../UTIL.SISA"
#include "aux.SISA"

init:
    CLIR, sp, STACK_START;
    


start:
    CLIR, t5, 0x24;
    MOV(arg0, t5);
    NOP;
    NOP;
    CLIR, t1, 23;
    CLIR, t2, 44;
    NOP;
    NOP;
    NOP;  
    JMP, end;


end:
   END_SIMUL;
   JMP, end;